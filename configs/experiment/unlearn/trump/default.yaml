# @package _global_

defaults:
  - override /model: Llama-3.2-3B-Instruct
  - override /trainer: TDU
  - override /data: unlearn
  - override /data/datasets@data.forget: trump_forget
  - override /data/datasets@data.retain: general_retain
  - override /eval: null

model:
  model_args:
    pretrained_model_name_or_path: meta-llama/Llama-3.2-3B-Instruct

data:
  anchor: forget

trainer:
  args:
    warmup_epochs: 0
    learning_rate: 1e-5
    weight_decay: 0.01
    num_train_epochs: 10

task_name: ???
